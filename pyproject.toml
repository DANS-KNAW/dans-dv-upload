[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dans-dv-upload"
version = "0.1.0"
dependencies = [
    "requests",
    "pyyaml",
]

[tool.setuptools.package-dir]
"" = "src/main/python"
